# php-pdo-mysql-class

## 초기화
```
<?php
define('DBHost', '127.0.0.1');
define('DBName', 'Database');
define('DBUser', 'root');
define('DBPassword', '');
$DB = new DB(DBHost, DBName, DBUser, DBPassword);
?>
```
